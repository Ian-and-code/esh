// split.hpp
#pragma once
#include <string>
#include <vector>

// Función que separa un string por un delimitador y devuelve un vector de strings
std::vector<std::string> split(const std::string& s, char delimiter);
